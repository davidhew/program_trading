1.pandas需要的一些计算库，最多只支持python 3.12；不能支持3.14

#扫描项目代码，安装依赖包
pip install pipreqs
pipreqs ./ --encoding=utf8 --force --ignore .venv

#会将最全面的依赖库信息写入到requirements.txt里
pip freeze > requirements.txt
pip install --upgrade pip  # 先升级 pip 自身，确保支持 3.12 的最新特性
pip install -r requirements.txt



# 允许 docker 网桥流量转发
iptables -I FORWARD -i docker0 -j ACCEPT
iptables -I FORWARD -o docker0 -j ACCEPT

# 强制对 docker 网段进行 NAT 伪装
# 注意：如果你的 docker 网段不是 172.17.0.0，请修改
iptables -t nat -I POSTROUTING -s 172.17.0.0/16 -j MASQUERADE

#停止并删除容器
docker stop stock_analyzer
docker rm stock_analyzer

#构建&运行容器
# 1. 强制重新构建镜像（这步会下载依赖，但只执行一次）
docker-compose up -d --build

docker-compose build --build-arg http_proxy=http://host.docker.internal:7890 \
             --build-arg https_proxy=http://host.docker.internal:7890 \
             -t stock_app-quant_bot .

# 2. 启动容器
docker-compose up -d

#在mac端为python项目下载基于x86架构依赖的所有python包到本地
pip download -r requirements.txt \
    -d ./packages \
    --platform manylinux_2_27 \
    --only-binary=:all: \
    --python-version 312

##在mac端为python项目下载基于Mac Mini 上的 Docker 运行 Linux 容器的所有python包到本地
pip download -r requirements-mac-mini-docker.txt \
    -d ./packages_mac_m1 \
    --platform manylinux2014_aarch64 \
    --platform manylinux_2_17_aarch64 \
    --platform manylinux_2_28_aarch64 \
    --only-binary=:all: \
    --python-version 312

#查看docker实例的日志路径
docker inspect --format='{{.LogPath}}' <容器ID或容器名>

#排查问题相关
#查看正在运行的容器（最常用）
docker ps

#查看实时日志
docker logs -f stock_analyzer

|----修改了什么？------------|是否需要 Build？|---------	原因-------------------------|
| Python 代码 (.py 文件)    |	   否      |因为有 volumes 挂载，容器直接读取宿主机文件。  |
|依赖清单 (requirements.txt)|	   是	   |    必须重新安装新包并打包进镜像层。          |
|    Dockerfile本身        |	       是	   |    镜像的构建逻辑发生了变化。               |
|环境变量(Compose里的environment)|	否	   | 只要up -d 重启容器,Compose会自动注入新变量。 |


#tailscale相关

open /Applications/Tailscale.app

tailscale status

tailscale up

tailscale down


###mac mini设置成服务器模式（尽量不主动进入休眠状态）
Bash
# 1. 无论是否关闭显示器，禁止系统进入睡眠
sudo pmset -a sleep 0

# 2. 禁止硬盘进入待机睡眠模式
sudo pmset -a disksleep 0

# 3. 启用“唤醒以进行网络访问”（确保 SSH 随时能连上）
sudo pmset -a womp 1

# 4. 启用“断电后自动重新启动”（服务器必备：停电后来电会自动开机）
sudo pmset -a autorestart 1

# 5. 禁用掉电后的自动关机功能（如果是笔记本或带 UPS 的 Mac）
sudo pmset -a proximitywake 1

网络相关问题：
1.在mac mini上，如果使用非本地的代理，对于docker的网络好像会造成问题；所以如果需要代理，一定在mac mini上本地把代理搞起来